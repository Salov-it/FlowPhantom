
# 📘 FlowPhantom — Полная Техническая Документация

## 🧩 Содержание
1. Введение
2. Архитектура системы
3. Поток трафика (end‑to‑end data flow)
4. Компоненты системы
5. Протокол FlowPhantom
6. Маскировка под видео VK
7. Сервер FlowPhantom.Server
8. Клиент FlowPhantom.Client (Windows / Desktop)
9. Виртуальный адаптер WinTUN
10. TunnelManager
11. Взаимодействие с реальными VPN-клиентами (V2RayTun / Outline / WireGuard)
12. NAT Router
13. FlowPhantom для Android (план)
14. Сценарии использования
15. Установка и развёртывание
16. Форматы пакетов
17. Будущие улучшения


# 1. Введение

FlowPhantom — это экспериментальный VPN-транспорт, создающий **маскировку сетевого трафика под видеостриминг VK Video**.  
Цель — обход DPI, который блокирует VPN-трафик, но не хочет блокировать трафик ВКонтакте.

FlowPhantom работает как **прослойка**:

- На клиенте создаёт TUN‑интерфейс
- Перехватывает IP-трафик
- Заворачивает его в собственный протокол
- Маскирует структуру как сегменты видео VK
- Отправляет на сервер
- Сервер снимает маскировку и пересылает трафик в интернет
- Ответы маскируются обратно и уходят в клиент



# 2. Архитектура системы

```
┌─────────────────────────┐
│  Приложения (Chrome, TG) │
└──────────────┬──────────┘
               │
      (IP пакеты)
               │
      ┌─────────────────┐
      │ WinTUN Adapter  │   ← виртуальный сетевой интерфейс
      └────────┬────────┘
               │
               ▼
      ┌─────────────────┐
      │ FlowPhantom.Client 
      │  - PacketFramer
      │  - MediaSegmentCodec
      │  - MaskEncoder (VK)
      └────────┬────────┘
               │   TCP 443 mimic
               ▼
      ┌─────────────────┐
      │ FlowPhantom.Server
      │  - MaskDecoder
      │  - Reassembler
      │  - NAT Router
      └────────┬────────┘
               │
     (Интернет: DNS, TCP, UDP)
               │
               ▼
      ┌─────────────────┐
      │ Реальный сайт / VPN‑сервер │
      └─────────────────┘
```


# 3. Поток трафика (end-to-end workflow)

## Клиент → Сервер

1. IP‑пакет от приложения попадает в WinTUN
2. TunnelManager передает пакет FlowClient
3. FlowClient:
   - режет на чанки (до 1200 байт)
   - упаковывает в PacketFramer
   - оборачивает в MediaSegment
   - маскирует MaskEnvelopeEncoder
   - отправляет как TCP frame (length‑prefixed)

## Сервер → Интернет
4. Сервер получает TCP фрейм
5. Снимает маскировку
6. Реконструирует PacketFramer
7. NAT Router отправляет пакет в реальный интернет
8. Получает ответ

## Сервер → Клиент
9. Оборачивает ответ в PacketFramer
10. Создаёт MediaSegment
11. Маскирует под VK
12. Возвращает клиенту как TCP frame

## Клиент → ОС
13. Клиент получает фрейм
14. Снимает маскировку
15. Данные уходят в WinTUN
16. ОС получает "как будто от VPN".


# 4. Компоненты системы

## FlowPhantom.Client
- WinTUN Device
- PacketFramer
- MediaSegmentCodec
- MaskEncoder
- ReceiverLoop / SenderLoop
- TunnelManager

## FlowPhantom.Server
- TCP listener
- MaskEnvelopeDecoder
- MediaSegmentCodec
- Reassembler (собирает сегменты)
- PacketFramer
- NAT Router (UDP)
- (В будущем TCP‑NAT)

## FlowPhantom.Infrastructure
- Общие классы
- Парсер IP-пакетов
- Utils


# 5. Протокол FlowPhantom

## Формат отправки

```
[length:4 bytes big-endian][mask_frame]
```

### Внутри mask_frame:

MaskEnvelopeEncoder (структура аналогична сегментам видео VK):

```
MAGIC (4 bytes)
FrameType (1 byte)
SegmentId (4 bytes)
PayloadLength (4 bytes)
PaddingLength (4 bytes)
Payload (...)
Padding (...)
```

### Внутри Payload:

MediaSegmentCodec:

```
StreamId (4 bytes)
SegmentIndex (4 bytes)
Pts (4 bytes)
Duration (4 bytes)
PacketFramerFrame (...)
```

### Внутри PacketFramerFrame:

```
[Length:2 bytes]
[Flags:1 byte]
[Session:2 bytes]
[Payload:... raw IP packet ...]
```


# 6. Маскировка под видео VK

### MaskEnvelopeEncoder имитирует:

- переменную длину чанков (от 1000 до 5000 байт)
- паддинг случайного размера
- похожие заголовки
- структуру "медиасегмента"
- временные задержки между сегментами
- паттерн chunked streaming

DPI не может отличить это от mp4/hls сегментов.

Трафик отправляется через **TCP**, как у VK Video (HTTPS).


# 7. Сервер FlowPhantom.Server

Реализует:

- TCP listener (5001)
- декодирование фрейма
- Reassembler для сегментов
- NAT Router
- обратное кодирование

Поддержка:
- UDP (DNS, QUIC)
- в будущем TCP (полный туннель)


# 8. Клиент FlowPhantom.Client

Поддерживает:

- WinTUN
- сбор IP пакетов
- отправка по TCP как видеосегменты
- приём фреймов
- декодирование
- запись пакетов в WinTUN

Откуда берётся трафик:
- Chrome
- Telegram
- Steam
- Любые приложения


# 9. WinTUN Adapter

Применён драйвер Wintun от WireGuard.

Создаётся вручную:

```
var adapter = Wintun.CreateAdapter("FlowPhantom", "FlowPhantomTun");
```

Программа сама настраивает IP + маршруты:

```
10.99.0.2 — клиент
10.99.0.1 — сервер
```


# 10. TunnelManager

Соединяет:

```
WinTUN → FlowClient.SendAsync
FlowClient.OnMessage → WinTUN.WritePacket
```


# 11. NAT Router

UDP-forwarding:

- принимает IPv4+UDP пакет
- отправляет его в интернет
- получает ответ
- строит обратный IPv4 пакет
- возвращает клиенту

Поддерживает DNS, STUN, QUIC handshake.


# 12. FlowPhantom для Android — план реализации

Основан на:

- Android VPNService (аналог WinTUN)
- Kotlin/Java/C#
- FlowPhantom.Client порт
- MaskEncoder полностью совместимый

Цель: чтобы телефон → сервер отправлял **маскированный трафик**, а DPI видел VK.


# 13. Сценарии использования

### 1. Обход сотового DPI
- МТС
- Beeline
- Tele2
- Мегафон

### 2. Маскировка VPN
Можно пропускать:
- WireGuard
- Shadowsocks
- VMess/VLESS
- OpenVPN
- Trojan

### 3. Маскировка корпоративного DPI
FlowPhantom может использоваться для тестирования DPI фильтров.


# 14. Установка и развёртывание

### Клиент Windows

```
scripts/install-flowphantom-client.bat
```

### Сервер Linux

```
scripts/install-flowphantom-server.sh
```


# 15. Форматы пакетов

Включены все структуры:

- PacketFramer
- MediaSegmentMeta
- MediaSegmentCodec
- MaskEnvelopeEncoder
- MaskEnvelopeDecoder
- IPPacket.Parser
- UdpNatRouter


# 16. Будущие улучшения

- TCP NAT
- Multipath upload (как у QUIC)
- Mobile Android Client
- Поддержка iOS (NEPacketTunnel)
- Scramble TLS fingerprint
- Имитация HLS playlists
- Машинное обучение для имитации паттернов VK Video CDN


